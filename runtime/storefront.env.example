NEXT_PUBLIC_MEDUSA_BACKEND_URL=http://localhost:9000
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=obtain-from-medusa-admin
NEXT_PUBLIC_DEFAULT_REGION=dk

# Keep FUNNELMETRY_BACKEND_SIGNING_KEY outside this browser-facing file. Compose
# injects it into the storefront server runtime from the server-only env file.
